name: my_pills
description: "MyPills — medication tracker. Client-only Flutter MVP."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

# Dependency notes:
#   drift / drift_flutter / drift_dev  — local DB, analogous to EF Core
#   flutter_riverpod / riverpod_*      — state management / DI container
#   go_router                           — declarative navigation
#   freezed_annotation / freezed        — immutable records (≈ C# records)
#   json_annotation / json_serializable — JSON codegen
#   sqlite3_flutter_libs                — native SQLite bindings
dependencies:
  drift: ^2.31.0
  drift_flutter: 0.2.8
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^3.3.1
  freezed_annotation: ^3.1.0
  go_router: ^14.8.1
  intl: ^0.20.2
  json_annotation: ^4.9.0
  path_provider: ^2.1.5
  riverpod_annotation: 4.0.2
  sqlite3_flutter_libs: ^0.5.24

dev_dependencies:
  build_runner: ^2.4.15
  drift_dev: ^2.31.0
  flutter_test:
    sdk: flutter
  freezed: ^3.2.5
  json_serializable: ^6.9.5
  mocktail: ^1.0.4
  riverpod_generator: 4.0.3
  very_good_analysis: ^10.2.0

flutter:
  uses-material-design: true

  generate: true   # enables flutter gen-l10n

  assets:
    - assets/fonts/

  fonts:
    - family: Manrope
      fonts:
        - asset: assets/fonts/Manrope-ExtraLight.ttf
          weight: 200
        - asset: assets/fonts/Manrope-Light.ttf
          weight: 300
        - asset: assets/fonts/Manrope-Regular.ttf
          weight: 400
        - asset: assets/fonts/Manrope-Medium.ttf
          weight: 500
        - asset: assets/fonts/Manrope-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Manrope-Bold.ttf
          weight: 700
        - asset: assets/fonts/Manrope-ExtraBold.ttf
          weight: 800
